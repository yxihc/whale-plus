import { withInstall } from '@whale-plus/utils';
import ConfigProvider from './src/config-provider.vue';

export const WpConfigProvider = withInstall(ConfigProvider);
export default WpConfigProvider;
